name: measure_metrics.yml
on:
  push:
    branches:
      - dev

jobs:
  analyze:
    name: Code Metrics Analysis
    runs-on: ubuntu-latest
    steps:
      - name: Start Energy Measurement
        uses: green-coding-solutions/eco-ci-energy-estimation@v4
        with:
          json-output: true
          task: start-measurement
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: |
          npm install -g typescript
          npm install -g eslint
          npm install -g @typescript-eslint/parser
          npm install -g @typescript-eslint/eslint-plugin
          npm install -g eslint-plugin-sonarjs
          npm install -g cloc
          npm install typescript-metrics
      - id: measurement-4
        name: Record Measurement After Install dependencies
        uses: green-coding-solutions/eco-ci-energy-estimation@v4
        with:
          json-output: true
          label: Install dependencies
          task: get-measurement
      - name: Setup ESLint config
        run: |
          echo '{
            "parser": "@typescript-eslint/parser",
            "plugins": ["@typescript-eslint", "sonarjs"],
            "extends": [
              "plugin:@typescript-eslint/recommended",
              "plugin:sonarjs/recommended"
            ]
          }' > .eslintrc.json
      - name: Run ESLint Analysis
        run: |
          echo "=== ESLint Analysis ===" > metrics_report.txt
          eslint . --ext .ts --format stylish >> metrics_report.txt || true
      - id: measurement-6
        name: Record Measurement After ESLint Analysis
        uses: green-coding-solutions/eco-ci-energy-estimation@v4
        with:
          json-output: true
          label: ESLint Analysis
          task: get-measurement
      - name: Count Lines of Code
        run: |
          echo -e "\n=== Lines of Code Analysis ===" >> metrics_report.txt
          cloc . --include-lang=TypeScript >> metrics_report.txt
      - id: measurement-8
        name: Record Measurement After Lines of Code Analysis
        uses: green-coding-solutions/eco-ci-energy-estimation@v4
        with:
          json-output: true
          label: Lines of Code Analysis
          task: get-measurement
      - name: Generate SonarJS Analysis
        run: |
          echo -e "\n=== SonarJS Analysis ===" >> metrics_report.txt
          eslint . --ext .ts --plugin sonarjs --format json >> metrics/sonar_analysis.json || true
      - id: measurement-10
        name: Record Measurement After SonarJS Analysis
        uses: green-coding-solutions/eco-ci-energy-estimation@v4
        with:
          json-output: true
          label: SonarJS Analysis
          task: get-measurement
      - name: Generate detailed reports
        run: |
          mkdir -p metrics
          cp metrics_report.txt metrics/full_report.txt
      - name: Upload metrics results
        uses: actions/upload-artifact@v4
        with:
          name: code-metrics
          path: metrics/
      - id: display-measurement
        name: Display Energy Results
        uses: green-coding-solutions/eco-ci-energy-estimation@v4
        with:
          json-output: true
          task: display-results
      - name: Save Total Energy Consumption Data
        run: echo '${{ steps.final-measurement.outputs.data-total-json }}' > total_energy_consumption.json
      - name: Upload Energy Consumption Artifact
        uses: actions/upload-artifact@v4
        with:
          name: total-energy-consumption
          path: total_energy_consumption.json